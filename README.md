# [photos.coderbyheart.com](https://photos.coderbyheart.com/)

Website for my photos.

## Node & NPM

This project requires Node.js `>=24.20.0 <25` and npm `>=12.0.2 <13` (enforced
via `check-node-version` on `npm install` and `npm ci`).
